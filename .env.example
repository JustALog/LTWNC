# Environment variables configuration
HOST=localhost
PORT=3000

# Database configuration
DB_HOST=localhost 
DB_PORT=3306
DB_USER=root
DB_PASSWD=root
DB_NAME=studentreg

# JWT configuration
JWT_SECRET=ltwnc-jwt-secret-key-2026